- resolve ULRs like `lodash/get` 
- package must build into all formats 'iife', 'cjs' and 'esm'. For now only `esm`
- dynamic imports
- make available transforms
- parse Vue components: `import {parse} from @vue/compiler-sfc`

